{
    "name": "Linux Script Settings",
    "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
    "features": {
        "ghcr.io/devcontainers/features/docker-in-docker:2": {
            "version": "latest",
            "moby": true
        }
    },
    "customizations": {
        "vscode": {
            "settings": {
                "terminal.integrated.defaultProfile.linux": "bash",
                "editor.formatOnSave": true
            },
            "extensions": [
                "mads-hartmann.bash-ide-vscode",
                "redhat.vscode-yaml",
                "ms-azuretools.vscode-docker",
                "irongeek.vscode-env"
            ]
        }
    },
    "forwardPorts": [80, 8080, 3306],
    "portsAttributes": {
        "80": { "label": "Apache (HTTP)" },
        "8080": { "label": "Dev Server" },
        "3306": { "label": "MariaDB (MySQL)" }
    },
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y git vim tree htop net-tools dnsutils build-essential bc && chmod +x ./.devcontainer/setup-env.sh",
    "postAttachCommand": "git pull && /bin/bash ./.devcontainer/setup-env.sh && . ./bin/repo.sh"
}
